---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-white text-gray-800">
		<div class="container mx-auto px-4 py-6">
			<div class="flex justify-between items-center mb-2" id="tarmenu">
				<div class="flex items-center space-x-2">
					<a href="/tars" class="cursor-pointer">
						<div class="w-10 h-10 rounded-full overflow-hidden">
							<img src="/iyothee.jpg" alt="Profile" class="w-full h-full object-cover">
						</div>
					</a>
					<div class="flex flex-col">
						<span class="text-sm font-medium text-gray-700">thamizhi</span>
						<span class="text-xs font-medium text-green-500">work</span>
					</div>
				</div>
				<div class="flex space-x-4">
					<a href="/orai" class="cursor-pointer">
						<svg class="h-5 w-5 text-gray-300 hover:text-gray-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<polygon points="12 2 22 22 2 22" />
						</svg>
					</a>
					<a href="/tar" class="cursor-pointer">
						<svg class="h-5 w-5 text-gray-300 hover:text-gray-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<rect x="3" y="3" width="18" height="18" rx="2" ry="2" />
						</svg>
					</a>
					<a href="/a" class="cursor-pointer">
						<svg class="h-5 w-5 text-gray-300 hover:text-gray-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<circle cx="12" cy="12" r="10" />
						</svg>
					</a>
				</div>
			</div>
			<slot />
		</div>
	</body>
</html>

<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	html {
		font-family: system-ui, sans-serif;
		background-size: 224px;
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}
</style>
